---
title: "Desafio_01"
author: "Amanda Cedro"
format: 
  html:
    self-contained: true
editor: visual
---

```{r}
library(reticulate)
#virtualenv_create("ambiente")
```

```{r}
use_virtualenv("ambiente")
py_install("pandas", envname = "ambiente")
```

```{r}
start <- Sys.time()
flights <- read.csv(unz('dados/flights.csv.zip', 'flights.csv'))
end <- Sys.time()
end - start
```

```{r}
library(readr)
start <- Sys.time()
flights <- read_csv(unz('dados/flights.csv.zip', 'flights.csv'))
end <- Sys.time()
end - start
```

```{python}
import time
import pandas as pd
from zipfile import ZipFile
start = time.time()
with ZipFile("dados/flights.csv.zip", 'r') as zip_file:
    # Lê o arquivo flights.csv
    with zip_file.open("flights.csv") as file:
        flights = pd.read_csv(file, header=0, sep=",", low_memory=False)
end = time.time()
end - start
```
